<Page x:Class="WpfApp1.Pages.MainWindowPage.Client.SelectProdListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages.MainWindowPage.Client"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SelectProdListPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.25*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel>

            <TextBlock  Text="Введите количество:" />


            <TextBlock />
            <TextBox x:Name="TBoxAmount" />
            <TextBlock />
            <Button Click="Button_Click" Content="Выбрать" Background="Gold" FontSize="18" />
            <TextBlock />
            <Button Click="BackClick" Content="Назад" Background="Gold" FontSize="18" />
        </StackPanel>

        <Frame Grid.Column="1" x:Name="FrameInWindow" />
    </Grid>
</Page>
